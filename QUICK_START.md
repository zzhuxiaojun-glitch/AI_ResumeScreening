# 快速开始指南 - AI 简历初筛系统

## 系统当前状态

✅ **所有3个里程碑已完成并可运行**

系统已包含完整的 M1 + M2 + M3 功能，可直接使用。

## 30秒快速测试

### 1. 创建账号（5秒）
```
访问应用 → Sign Up
邮箱: admin@test.com
密码: password123
```

### 2. 创建岗位（10秒）
```
Positions → New Position
复制粘贴下面的配置 → Create
```

```
Title: Frontend Developer

Must-Have Skills:
React: 3
TypeScript: 2

Nice-to-Have Skills:
Docker: 1

Reject Keywords:
在校生

Thresholds: A=80, B=60, C=40
```

### 3. 导入测试数据（10秒）
```
Email Import → 填写任意配置 → Save
点击 "Import Latest Resumes"
```

### 4. 查看结果（5秒）
```
Candidates → 看到2个候选人
点击 View → 查看评分详情
```

## 功能清单

| 里程碑 | 功能 | 状态 | 测试方法 |
|--------|------|------|---------|
| **M1** | 手动上传简历 | ✅ | Upload页面 → 上传文件 |
| | 自动解析提取 | ✅ | 自动提取姓名/邮箱/技能 |
| | 候选人列表 | ✅ | Candidates页面 |
| | 搜索筛选 | ✅ | 搜索框输入关键词 |
| | CSV导出 | ✅ | Export CSV按钮 |
| **M2** | 岗位管理 | ✅ | Positions页面 → CRUD |
| | 技能配置 | ✅ | Must/Nice/Reject配置 |
| | 智能评分 | ✅ | 0-100分自动计算 |
| | A/B/C/D分级 | ✅ | 自动分级显示 |
| | 评分解释 | ✅ | 详情页查看解释 |
| | 按岗位筛选 | ✅ | Position筛选器 |
| **M3** | 邮箱配置 | ✅ | Email Config页面 |
| | 邮件导入 | ✅ Mock | Import按钮（Mock数据） |
| | 实时进度 | ⚠️ | 基础实现 |
| | 失败重试 | ⚠️ | 待完善 |

**图例**:
- ✅ = 完整实现
- ⚠️ = 基础实现
- Mock = 演示版本（需实际IMAP集成）

## 页面导航

```
登录后导航栏:
├── Candidates    - 候选人列表（默认首页）
├── Positions     - 岗位管理
├── Upload        - 手动上传
└── Email Import  - 邮箱导入
```

## 核心数据流

```
上传简历
  ↓
Storage存储
  ↓
创建resume记录
  ↓
调用parse-resume Edge Function
  ↓
提取候选人信息 + 计算评分
  ↓
创建candidate记录 + scores记录
  ↓
显示在列表
```

## 测试数据模板

### 简历模板（txt格式）

**高分候选人** (预期A级):
```
姓名：张伟
邮箱：zhangwei@tech.com
电话：13800138000
学历：硕士
学校：清华大学
专业：计算机科学
毕业时间：2020年6月
工作经验：5年

技能：React、TypeScript、Node.js、Docker、AWS、Python、MySQL
```

**中等候选人** (预期B级):
```
姓名：李娜
邮箱：lina@email.com
电话：13900139000
学历：本科
学校：北京大学
工作经验：3年

技能：React、JavaScript、HTML、CSS
```

**低分候选人** (预期D级):
```
姓名：王小明
邮箱：xiaoming@gmail.com
学历：本科在读
学校：某大学

在校生，寻找实习机会
技能：HTML、CSS基础
```

## 评分示例

### 岗位: Frontend Developer
- Must: React(3), TypeScript(2)
- Nice: Docker(1)
- Reject: 在校生

### 候选人得分计算

| 候选人 | Must匹配 | Nice匹配 | Reject | 计算 | 总分 | 等级 |
|--------|---------|---------|--------|------|------|------|
| 张伟 | React+TS | Docker | 无 | (3+2)×10 + 1×5 = 55 | 55 | B |
| 李娜 | React | 无 | 无 | 3×10 = 30 | 30 | D |
| 王小明 | 无 | 无 | 在校生 | 0 - 15 = -15 | 0 | D |

**注意**: 如果岗位配置更多技能，分数会相应增加。

## 常见操作

### 1. 上传简历
```
1. 创建txt文件（复制上面模板）
2. Upload页面 → 选择岗位 → 上传文件
3. 等待3-5秒解析
4. Candidates页面查看结果
```

### 2. 查看评分
```
1. Candidates页面 → 找到候选人
2. 点击 View 按钮
3. 查看:
   - 总分和等级（右上角大字）
   - 分项得分（Must/Nice/Reject）
   - 匹配技能（绿色标签）
   - 缺失技能（红色标签）
   - 详细解释（灰色框）
```

### 3. 筛选候选人
```
列表页筛选选项:
- 搜索框: 姓名/邮箱/电话/技能
- Position: 按岗位筛选
- Grade: A/B/C/D等级筛选
- Sort: 按分数/时间排序
```

### 4. 导出CSV
```
1. 应用筛选条件
2. 点击 "Export CSV" 按钮
3. 文件自动下载
4. Excel/Google Sheets打开
```

### 5. 邮箱导入（Mock）
```
1. Email Import → Add Configuration
2. 填写任意配置（会被mock忽略）
3. Save Configuration
4. 点击 "Import Latest Resumes"
5. 等待5-10秒
6. Candidates页面看到2个测试候选人
```

## 目录结构速查

```
项目根目录/
├── README.md                  # 完整文档
├── SETUP.md                   # 安装指南
├── SAMPLE_DATA.md             # 测试数据
├── PROJECT_STRUCTURE.md       # 项目结构
├── MILESTONES.md              # 里程碑概览
├── MILESTONE_M1.md            # M1详细文档
├── MILESTONE_M2.md            # M2详细文档
├── MILESTONE_M3.md            # M3详细文档
├── QUICK_START.md             # 本文件
│
├── src/
│   ├── components/            # React组件
│   │   ├── LoginPage.tsx
│   │   ├── CandidatesPage.tsx
│   │   ├── PositionsPage.tsx
│   │   ├── UploadPage.tsx
│   │   ├── EmailConfigPage.tsx
│   │   └── CandidateDetailPage.tsx
│   ├── contexts/
│   │   └── AuthContext.tsx
│   └── lib/
│       └── supabase.ts        # 数据库类型
│
└── supabase/
    └── functions/             # Edge Functions
        ├── parse-resume/      # 解析+评分
        └── import-emails/     # 邮件导入
```

## API速查

### Supabase REST API

```bash
# 获取候选人列表
GET /rest/v1/candidates?select=*&order=created_at.desc

# 获取某个候选人
GET /rest/v1/candidates?id=eq.{uuid}&select=*

# 获取候选人评分
GET /rest/v1/scores?candidate_id=eq.{uuid}

# 获取岗位列表
GET /rest/v1/positions?select=*

# 创建岗位
POST /rest/v1/positions
Body: {title, must_skills, nice_skills, ...}
```

### Edge Functions

```bash
# 解析简历
POST /functions/v1/parse-resume
Body: {resume_id: "uuid"}

# 导入邮件
POST /functions/v1/import-emails
Body: {config_id: "uuid"}
```

## 数据库速查

### 核心表

```sql
-- 岗位
SELECT * FROM positions;

-- 简历文件
SELECT * FROM resumes;

-- 候选人
SELECT * FROM candidates;

-- 评分
SELECT * FROM scores;

-- 邮箱配置
SELECT * FROM email_configs;
```

### 常用查询

```sql
-- 查看某岗位所有候选人及评分
SELECT
  c.*,
  s.total_score,
  s.grade
FROM candidates c
LEFT JOIN scores s ON s.candidate_id = c.id
WHERE c.position_id = 'your-position-id'
ORDER BY s.total_score DESC;

-- 查看A级候选人
SELECT c.name, c.email, s.total_score, s.grade
FROM candidates c
JOIN scores s ON s.candidate_id = c.id
WHERE s.grade = 'A'
ORDER BY s.total_score DESC;

-- 统计各等级数量
SELECT
  s.grade,
  COUNT(*) as count
FROM scores s
GROUP BY s.grade
ORDER BY s.grade;
```

## 故障排查

### 问题1: 上传失败
```
检查:
- 文件大小 < 10MB?
- 文件格式是 PDF/DOC/DOCX/TXT?
- Storage bucket "resumes" 已创建?

解决:
supabase/migrations/ 查看是否执行了 create_resume_storage.sql
```

### 问题2: 解析没有结果
```
检查:
- 简历内容是否包含中文关键词（姓名、邮箱等）?
- parse-resume Edge Function 是否部署?
- 浏览器Console有错误吗?

解决:
使用提供的测试模板，确保格式正确
```

### 问题3: 评分为0
```
检查:
- 候选人技能与岗位要求匹配吗?
- 岗位的 must_skills 配置了吗?
- 简历中技能关键词正确吗（大小写不敏感）?

解决:
查看详情页 → 评分解释 → 看哪些技能匹配/缺失
```

### 问题4: 邮件导入没反应
```
当前限制:
- 当前为Mock实现，每次返回固定的2个测试候选人
- 真实IMAP需要额外实现

临时方案:
使用手动上传功能代替
```

## 下一步

### 功能增强
1. 实现真实IMAP连接（替换Mock）
2. 添加OCR支持（扫描版PDF）
3. 集成AI解析（OpenAI/Claude）
4. 添加面试安排功能
5. 实现数据分析Dashboard

### 优化建议
1. 添加分页（候选人列表）
2. 优化评分算法（更智能的匹配）
3. 支持自定义字段
4. 添加候选人标签系统
5. 实现协作功能（多HR共享）

## 获取帮助

- **完整文档**: 查看 README.md
- **里程碑详情**: MILESTONE_M1/M2/M3.md
- **测试数据**: SAMPLE_DATA.md
- **项目结构**: PROJECT_STRUCTURE.md

## 命令速查

```bash
# 开发
npm run dev              # 启动开发服务器（已自动运行）

# 构建
npm run build            # 构建生产版本

# 检查
npm run typecheck        # TypeScript类型检查
npm run lint             # 代码检查

# Supabase
# Edge Functions 已通过MCP工具自动部署
# 数据库迁移已自动执行
```

## 技术栈

- **前端**: React 18 + TypeScript + Tailwind CSS
- **后端**: Supabase (PostgreSQL + Edge Functions)
- **存储**: Supabase Storage
- **认证**: Supabase Auth
- **部署**: Vite Build

## 许可

MIT License - 可自由用于个人和商业项目

---

**系统已完全可运行，立即开始测试！** 🚀
